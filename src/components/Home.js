import React, { Component, propTypes } from 'react';

export default class Home extends Component {
    render() {
        return (
            <div>
                App
            </div>
        );
    }
}